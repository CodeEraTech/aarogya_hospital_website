@extends('layouts.admin') @section('title','Edit patient') @section('heading','Edit patient') @section('content')<div class="form-page"><a class="back-link" href="{{ route('admin.patients.index') }}">← Back to Patients</a><section class="panel form-panel"><form method="post" action="{{ route('admin.patients.update',$patient) }}">@csrf @method('PUT')<div class="form-grid"><label><span>Patient ID</span><input value="{{ $patient->patient_id }}" disabled></label><label><span>Full name <b class="required">*</b></span><input name="name" value="{{ old('name',$patient->name) }}" required></label><label><span>Phone number <b class="required">*</b></span><input name="phone" value="{{ old('phone',$patient->phone) }}" required></label><label><span>Email</span><input type="email" name="email" value="{{ old('email',$patient->email) }}"></label></div><div class="form-actions"><button class="button button-primary">Save patient</button></div></form></section></div>@endsection
